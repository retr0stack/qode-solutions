/**
 * Геометрия знака Q — снята с логотипа и вынесена в один файл.
 * Ею пользуются и статичный знак в шапке, и анимированный Q в hero,
 * и генераторы favicon/og-картинки. Правится в одном месте.
 *
 * Система координат: viewBox 0 0 200 200.
 */
export const Q_GEOMETRY = {
  viewBox: "0 0 200 200",

  /** Кольцо задано осевой линией и толщиной обводки. */
  ring: {
    cx: 94,
    cy: 92,
    /** Радиус осевой линии: по ней идёт stroke толщиной `width`. */
    r: 58,
    width: 44,
    /** Внешний и внутренний радиусы — для тонких контуров при «отрисовке». */
    outer: 80,
    inner: 36,
  },

  /**
   * Хвост: трапеция под ~30°. Левый срез уходит внутрь контрформы,
   * правый — вертикальный и выходит за внешний край кольца.
   */
  tail: "M90 94 L178 150 L178 184 L72 125 Z",

  /** Вектор «вылета» хвоста при сборке знака: вдоль его оси, вниз-вправо. */
  tailOffset: { x: 26, y: 16 },
} as const;

/** Длина окружности — для stroke-dasharray при отрисовке контура. */
export function circumference(radius: number): number {
  return 2 * Math.PI * radius;
}

/** Стопы градиента бренда. Один источник для SVG и CSS. */
export const BRAND_STOPS = [
  { offset: "0%", color: "#00E0F0" },
  { offset: "45%", color: "#2B7FFF" },
  { offset: "100%", color: "#6B3BF5" },
] as const;
